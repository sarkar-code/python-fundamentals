### Copy this file to `.env` and fill in real credentials. Do NOT commit your `.env`.
# Example MongoDB connection string. Replace <username>, <password>, and <cluster>.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/?retryWrites=true&w=majority

# If you prefer a different connection string format, set MONGODB_URI accordingly.